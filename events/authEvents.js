// events/authEvents.js
const { EventEmitter } = require('events');
const authEvents = new EventEmitter();

// You could attach listeners here or in server.js
// Example listeners are added in server.js

module.exports = authEvents;
